# Static Web Starter

HTML/CSS/JS static site ready for Vercel deploy.

## Run locally

Use any static file server:

```bash
npx serve .
# or
python3 -m http.server 8080
```

## Deploy to Vercel

```bash
npx vercel
```
