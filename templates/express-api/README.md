# Express + TypeScript API Starter

Express server with TypeScript, CORS, JSON parsing, dotenv.

## Quick Start

```bash
npm install
npm run dev
```

## Endpoints

- `GET /api/health` — Health check
- `GET /api/hello` — Hello message

## Build

```bash
npm run build
npm start
```
