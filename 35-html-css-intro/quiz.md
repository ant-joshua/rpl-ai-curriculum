# Quiz: HTML & CSS Intro

<div class="quiz">

**1. Tag HTML mana yang digunakan untuk mendefinisikan paragraf?**

- [ ] `<h1>`
- [x] `<p>`
- [ ] `<div>`
- [ ] `<span>`

**2. Elemen semantic HTML5 yang tepat untuk navigasi adalah:**

- [ ] `<div class="nav">`
- [x] `<nav>`
- [ ] `<header>`
- [ ] `<aside>`

**3. CSS Selector `.btn-primary` akan memilih:**

- [ ] Semua elemen `<btn-primary>`
- [x] Semua elemen dengan class `btn-primary`
- [ ] Elemen dengan id `btn-primary`
- [ ] Elemen `<button>` pertama di halaman

**4. Box model CSS terdiri dari (dari dalam ke luar):**

- [ ] Margin → Border → Padding → Content
- [x] Content → Padding → Border → Margin
- [ ] Padding → Content → Border → Margin
- [ ] Border → Content → Padding → Margin

**5. Flexbox property `justify-content: center` akan:**

- [ ] Memusatkan item secara vertikal dalam container
- [x] Memusatkan item secara horizontal dalam container (main axis)
- [ ] Mengatur jarak antar item
- [ ] Membungkus item ke baris baru

**6. CSS Grid berbeda dari Flexbox karena:**

- [x] Grid mengatur layout dalam dua dimensi (baris dan kolom), Flexbox dalam satu dimensi
- [ ] Grid hanya bisa dipakai di mobile
- [ ] Flexbox lebih baru dari Grid
- [ ] Grid tidak bisa membuat layout responsif

</div>
